services:
  llm:
    extends:
      file: ./docker/services.compose.yml
      service: llm

  faster_whisper:
    extends:
      file: ./docker/services.compose.yml
      service: faster_whisper

  transcribo-backend:
    extends:
      file: ./docker/services.compose.yml
      service: transcribo-backend
    ports:
      - "8000:8000"
